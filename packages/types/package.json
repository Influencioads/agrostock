{
  "name": "@agrotraders/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types, enums and zod DTOs (single source of truth).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "node": "./dist/index.js",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node -e \"require('fs').writeFileSync('dist/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  }
}
