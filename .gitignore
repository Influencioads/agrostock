# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
build/
.next/
.vite/
.turbo/
*.tsbuildinfo

# env
.env
.env.*
!.env.example

# prisma
apps/api/prisma/*.db

# uploaded assets (product images etc.) — stored on local FS, not committed
apps/api/uploads/
apps/api/private-uploads/

# expo / react native
.expo/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# logs
*.log
npm-debug.log*
pnpm-debug.log*
tatus
backups/
tmp.mjs
apps/mobile.zip

# os / editor
.DS_Store
Thumbs.db
.idea/
*.local

# docker volumes (if mounted locally)
infra/volumes/

# Generated by `pnpm --filter @agrotraders/api taxonomy:sync` — a run report, not source.
apps/api/prisma/.taxonomy-report.csv
